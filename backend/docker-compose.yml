version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
      - "587:587"
    env_file:
      - .env
    
    volumes:
      - ./uploads:/app/uploads
    
    restart: unless-stopped
    
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
